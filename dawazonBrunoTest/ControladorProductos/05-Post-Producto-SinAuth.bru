meta {
  name: 05-Post-Producto-SinAuth
  type: http
  seq: 5
}

post {
  url: {{baseUrl}}/api/products
  body: none
  auth: none
}

tests {
  test("POST sin autenticación devuelve 401", function() {
    expect(res.status).to.equal(401);
  });
}
