meta {
  name: 08-Put-Producto-SinAuth
  type: http
  seq: 8
}

put {
  url: {{baseUrl}}/api/products/{{newProductId}}
  body: none
  auth: none
}

script:pre-request {
  if (!bru.getVar("newProductId")) {
    bru.setVar("newProductId", "1");
  }
}

tests {
  test("PUT sin autenticacion devuelve 401", function() {
    expect(res.status).to.equal(401);
  });
}
